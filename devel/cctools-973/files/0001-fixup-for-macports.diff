From 2c02c6aad84f75df9c599dc841f0ee39ca1203d0 Mon Sep 17 00:00:00 2001
From: Gcenx <38226388+Gcenx@users.noreply.github.com>
Date: Tue, 15 Mar 2022 21:37:31 -0400
Subject: [PATCH] Fixup for macports

---
 cctools/Makefile.in  |  2 +-
 cctools/configure    | 16 ----------------
 cctools/configure.ac | 16 ----------------
 3 files changed, 1 insertion(+), 33 deletions(-)

diff --git a/cctools/Makefile.in b/cctools/Makefile.in
index c425d82..ee190da 100644
--- a/cctools/Makefile.in
+++ b/cctools/Makefile.in
@@ -361,7 +361,7 @@ top_build_prefix = @top_build_prefix@
 top_builddir = @top_builddir@
 top_srcdir = @top_srcdir@
 @ISDARWIN_FALSE@SUBDIRS = libobjc2 libstuff libmacho ar as misc otool efitools ld64 man
-@ISDARWIN_TRUE@SUBDIRS = libstuff libmacho ar as misc otool efitools ld64 man
+@ISDARWIN_TRUE@SUBDIRS = libstuff libmacho ar as misc otool efitools man
 ACLOCAL_AMFLAGS = -I m4
 DSTROOT = 
 RC_OS = macos
diff --git a/cctools/configure b/cctools/configure
index 06e7668..09dcb4e 100755
--- a/cctools/configure
+++ b/cctools/configure
@@ -3141,12 +3141,6 @@ fi
 
 CXXFLAGS="$CXXFLAGS -std=c++1z"
 
-if test "x$build" = "x$host"; then
-  CFLAGS="$CFLAGS -isystem /usr/local/include -isystem /usr/pkg/include"
-  CXXFLAGS="$CXXFLAGS -isystem /usr/local/include -isystem /usr/pkg/include"
-  LDFLAGS="$LDFLAGS -L/usr/local/lib -L/usr/pkg/lib"
-fi
-
 CWARNINGS=""
 OBJCWARNINGS=""
 CXXWARNINGS=""
@@ -3204,16 +3198,6 @@ case "$CCVERSION" in
   ;;
 esac
 
-EXTRAFLAGS="$EXTRAFLAGS -DLD64_VERSION_NUM=609"
-
-case "$CXXVERSION" in
-  *clang* )
-    if test "x$isdarwin" = "xyes"; then
-      CXXFLAGS="$CXXFLAGS -stdlib=libc++"
-    fi
-  ;;
-esac
-
 EXTRAFLAGS="$EXTRAFLAGS -fblocks"
 
 case $host_cpu in
diff --git a/cctools/configure.ac b/cctools/configure.ac
index 2cf4b8a..4663531 100644
--- a/cctools/configure.ac
+++ b/cctools/configure.ac
@@ -24,12 +24,6 @@ fi
 
 CXXFLAGS="$CXXFLAGS -std=c++1z"
 
-if test "x$build" = "x$host"; then
-  CFLAGS="$CFLAGS -isystem /usr/local/include -isystem /usr/pkg/include"
-  CXXFLAGS="$CXXFLAGS -isystem /usr/local/include -isystem /usr/pkg/include"
-  LDFLAGS="$LDFLAGS -L/usr/local/lib -L/usr/pkg/lib"
-fi
-
 CWARNINGS=""
 OBJCWARNINGS=""
 CXXWARNINGS=""
@@ -80,16 +74,6 @@ case "$CCVERSION" in
   ;;
 esac
 
-EXTRAFLAGS="$EXTRAFLAGS -DLD64_VERSION_NUM=609"
-
-case "$CXXVERSION" in
-  *clang* )
-    if test "x$isdarwin" = "xyes"; then
-      CXXFLAGS="$CXXFLAGS -stdlib=libc++"
-    fi
-  ;;
-esac
-
 EXTRAFLAGS="$EXTRAFLAGS -fblocks"
 
 case $host_cpu in
-- 
2.32.0 (Apple Git-132)
